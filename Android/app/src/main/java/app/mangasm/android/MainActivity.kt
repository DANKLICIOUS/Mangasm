package app.mangasm.android

/**
 * Placeholder launcher activity.
 * Replace with Compose host when Android implementation begins.
 *
 * Auth: use [app.mangasm.android.auth.AuthPath] — Google Sign-In + Supabase,
 * not Apple App Store Connect keys.
 */
class MainActivity {
    // TODO: AppCompatActivity / ComponentActivity when Gradle project is generated.
}
